# Environment / secrets
.env

# Composer dependencies (installed during Docker build)
/vendor/

# Editor / node (if a build step is ever added)
node_modules/

# Runtime / uploads
/public/uploads/*
!/public/uploads/.gitkeep

# OS / editor junk
.DS_Store
Thumbs.db
*.log
